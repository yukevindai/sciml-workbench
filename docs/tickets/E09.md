# E09 — Scoped memory and compatible reuse

Implemented September 20, 2026 on the existing E03 changes. See
[memory and reuse](../agent-memory-reuse.md) for scope, provenance and limits.

- Added typed retained preferences, sourced provisional lessons, operator-only
  revision corrections and safe failure snapshot retrieval. Provider projections
  withhold arbitrary prose and test-derived assessments.
- Added exact audit/split/evidence reuse with versioned keys, retained-file
  verification, current policy/exposure checks, shared job linkage and zero new
  scientific attempts. Failed results and cross-protocol benchmarks are excluded.
- Added migration `0009` to preserve memory revisions and provenance.
- Added safe failed-attempt memory and retained legacy E03 action replay.

## Verification

- Initial combined Windows E09/E12/E03 suite: **31 passed, 31 skipped**
  (PostgreSQL variants). Expanded memory/replay checks: **10 passed, 10 skipped**.
- First Linux Python 3.12 / PostgreSQL 16 regression: **215 passed, 5 skipped,
  2 failed**. Both failures expected the existing released-comparison error text;
  the new final-exposure fence ran first. The release check now retains its
  existing precedence while the run-level fence remains enforced.
- Corrected Linux SQLite/PostgreSQL memory, evaluation, registry and metadata
  regression: **114 passed, 4 skipped** in 190.70 seconds. Skips were SQLite
  variants of PostgreSQL concurrency checks. Migration retention, corrected
  receipts, legacy action replay and shared budget accounting passed.
- All **49 schema/OpenAPI files** matched and `pip check` passed. The existing
  Starlette deprecation warning remains. Python compilation and diff checks pass.
- Final Linux SQLite/PostgreSQL evaluation, PDF/split reuse and adaptive-resealing
  checks: **30 passed** in 55.85 seconds. All **49 schemas** and `pip check` passed
  again. No live model or real research account was used.

No dependency pin or public HTTP schema change; agent runtime remains disabled.
Apply migration `0009` through normal setup before using memory writes. The
disposable `sciml-e09-e12-tests` containers, network and test volume were removed
after verification. No hosted deployment, commit or push was performed.
