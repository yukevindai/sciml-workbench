# C10 — Real scientific integration suite

Implemented `scripts/check_scientific_integration.py`: installed Git provenance, constraints and public API drift are checked before any test gate can pass. The focused targets exercise real audit, SciSplit, benchmark, PDF evidence and Failure Memory integrations, plus actor-aware outcomes, grounding, frozen evaluation partitions, reports and replay. No substituted scientific computation satisfies these real fixtures. `--all` is wired into CI for the complete backend suite after a fresh constrained install.

The new archive suite generates actual scientific bundles and invokes the replay CLI in another interpreter. It distinguishes valid reproduction from unsafe paths, case collisions, links, missing/corrupt inventory, projection/schema/graph corruption, incompatible pins, incomplete benchmark evidence, credential leakage and numerical mismatch. Metadata-only C07/C12 fixtures now explicitly expect export rejection rather than presenting fabricated bundles as complete science.

Local validation (Python 3.12, pinned constrained installation):

- Combined report/workflow/outcome/evaluation/reference/archive/split/benchmark/evidence/Failure Memory/public-surface suite: **169 passed, 55 skipped**.
- Final archive/split/publication regression slice: **65 passed, 13 skipped**. The full real workflow also passed after the publication changes, including byte-identical Failure Memory storage before/after replay.
- Earlier focused archive/split/outcome/evaluation run: **77 passed, 28 skipped** (overlapping coverage, not additive).
- Final archive-only suite: **22 passed**; subsequent exact string row-ID regression: **1 passed**. Counts overlap the runs above.
- Installed Git pins, dependency constraints and public interface snapshot: passed.
- Generated schema/OpenAPI drift: **47 files checked**, passed.

PostgreSQL fixture parameters were skipped because `TEST_DATABASE_URL` was unavailable. CI, hosted/browser checks and a newly installed local environment are not claimed as run. The current environment passed the pin/constraint check; CI defines the fresh-environment gate. Existing Starlette/FastAPI deprecation warnings remain.

Behavior, reproducible commands and scientific limitations: [report verification/replay](../report-replay.md).
