# D12 — Pause/cancel/crash enforcement

Implemented immediate dispatch fences, deadline-bounded pause drain, atomic
cancellation of solely owned jobs, shared consumer detachment, worker claim-loss
observation, and retained external/budget recovery records. Unknown imports retain
truthful unknown outcomes. Scientific publication cannot cross a committed job
cancellation fence. See the [runtime contract](../agent-scheduler.md).

Acceptance tests cover queued/running cancellation, multiple consumers, preserved
reservations, stale publication, supervised process interruption and restart after
scientific submission. Full adaptive graph recovery remains E08; hosted process-tree
and PostgreSQL acceptance must be exercised in Linux CI/D06, not inferred from
Windows development checks.

September 20 local verification: final scheduler/control/API/worker-interruption
and migration batch passed 35 tests (46 environment-dependent skips). Existing
publication, budget, recovery and tool regressions were exercised; timeout
classification and historical migration-fixture failures were fixed and passed
on rerun. A transient report filesystem failure also passed on isolated rerun.
