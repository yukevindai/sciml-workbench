# C11 — Evidence references and claim validation

Implemented and locally verified September 19, 2026, alongside C12 and the existing C07 working tree. See [evidence references](../evidence-references.md).

## Result

- Exact retained PDF/text/representation/excerpt hashes, Unicode-code-point spans, extraction identity and supported page anchors are verified before use. Absent page values remain null; image-only evidence remains unavailable without invented locators or OCR.
- Immutable named anchors support source-span declarations with project/input-scoped graph resolution. Claim sets retain typed evidence, benchmark and split dependencies.
- Metric references require actual matching finite values at the declared JSON Pointer and partition. Missing metrics are absent, not zero. Agent metric reads obey C12 quarantine.
- Reference checks do not assert semantic support. Claim categories, uncertainty and limitations remain explicit; unreviewed claims are not silently promoted. E11 owns independent semantic review.
- Report capture retains named anchors and claim sets; assembly rechecks their source bytes and metric values. Legacy artifacts remain unchanged.

## Verification

Final reference, metadata/migration and report suite: **38 passed, 48 skipped**. Skips cover PostgreSQL parameters and PostgreSQL-only checks without a test database URL. Tests use real public PDF ingestion with non-ASCII text, exact spans, null pages, missing/false anchors, unavailable scanned text, corrupt bytes, false metrics, scope violations, semantic-review refusal, immutable named anchors and report verification.

The combined C11/C12 integration suite passed **115 tests, 65 skipped** before the final anchor integration; the final reference/migration/report suite above covers that integration. Final evaluation/artifact/contract/read checks passed **103 tests, 48 skipped**. All 47 schema/OpenAPI files and four frontend generated files match. Nine browser contract tests, TypeScript, the Next.js production build, scientific pin/interface checks and whitespace checks pass. Two existing FastAPI/Starlette warnings remain.

## Handoff

Apply **0005** before updated services: named anchors share this migration with C12's evaluation/exposure tables. The services are trusted backend APIs; E03 must supply authoritative scopes. Reference validity does not verify physical truth, licensing or semantic support. No new evidence/claim write endpoint, OCR, dependency update, commit, push or deployment. PostgreSQL/Linux and hosted CI were not run.
