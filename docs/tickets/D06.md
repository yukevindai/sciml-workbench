# D06 — Hosted agent runtime

Status: runbook and local integration checks implemented; live Render acceptance
blocked by failed anonymous-access checks. No Render deployment or paid provider
call was performed.

September 26 follow-up: read-only anonymous checks again returned HTTP **200**
for `/healthz`, `/`, and `/api/projects` on the recorded Render URL. Only status
codes were retained; workspace contents were not inspected. The required
authentication rejection still fails. Administrative access, deployed revision,
and the target service remain unconfirmed. See the
[acceptance follow-up](../release/acceptance-2026-09-26.md).

The [Render runbook](../render-setup.md) now covers the public Next.js service,
private three-process backend, persistent disk and PostgreSQL checkpoints. It
documents server-only provider configuration, reviewed bounds/policies, opt-in
enablement, startup failures, polling/SSE reconnect, lease-aware redeploy recovery,
negative controls, persistence checks and schema-compatible rollback. The obsolete
public-login bypass instructions were removed to match D05's production gate.
Hosting restrictions were checked against Render's official documentation on
September 22, 2026.

The existing `workbench.serve` launcher already supplies independent API,
scientific-worker and optional agent-worker process groups with shared failure
handling. New tests verify configuration rejection before setup, migration/setup
ordering, optional third-child startup, failure of each child and platform SIGTERM
cleanup of all three children. CI's browser integration now starts this launcher
instead of starting API and scientific worker independently.

Reproduce focused acceptance on Linux with repository-constrained dependencies and
an isolated PostgreSQL `TEST_DATABASE_URL`:

```bash
python -m pytest backend/tests/test_hosting.py backend/tests/test_agent_runtime.py backend/tests/test_agent_acceptance.py backend/tests/test_agent_scheduler.py -q
cd frontend
node --test tests/private-boundary.test.mjs
```

The PostgreSQL restart fixture reopens durable checkpoints and executes a saved
proposal without another provider call. Provider generation remains deterministic
in these fixtures. It does not establish live model quality or actual Render
redeploy behavior. Follow section 7 of the runbook and retain the deployment SHA,
before/after IDs/checksums, event cursor, accounting and recovery time before marking
D06 hosted acceptance complete. Missing provider configuration blocks the live
agent checks; manual-mode persistence alone is insufficient. Coordinated restore
remains D07.

September 22 verification:

- Disposable Linux/Python 3.12/PostgreSQL acceptance: **46 passed, 2 skipped**.
  SQLite parameters skip PostgreSQL-only cases. Real three-child shutdown and
  PostgreSQL checkpoint restart checks passed. The test containers and network
  were removed afterward.
- Frontend production authentication/proxy boundary: **1 passed**.
- `git diff --check` passed. The changed CI browser startup has not been run on
  hosted CI in this ticket.
- Read-only anonymous probes of `https://sciml-web.onrender.com`: `/healthz`
  returned **200**, but `/` and `/api/projects` also returned **200**, failing
  the required production gate. Nonexistent scoped event/stream/download paths
  returned **404**, also without authentication rejection. Response bodies were
  discarded; no workspace contents were inspected or changed.

The deployed revision/configuration has not been identified. Confirm it and deploy
the reviewed production gate before repeating authenticated persistence/recovery
acceptance. The supplied URL does not establish Render administrative access or
provider readiness; those remain unverified. No commit or push was performed.
