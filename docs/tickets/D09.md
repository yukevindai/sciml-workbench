# D09 — Integrated CI gates

Status: implemented; full Linux/PostgreSQL/Compose CI execution pending.

The workflow now invokes B10's strict PostgreSQL runner, retains C10's pinned
real scientific fixtures and all backend regressions, and adds a separate
skip-rejecting replay/restore gate. Schema drift, E15 deterministic trajectories,
frontend checks/build/browser regressions and Compose smoke remain required.
A10 real-stack browser acceptance is now wired into the Compose job with
synthetic policy/provider configuration and the scripted coordinator.

Revision-specific receipts, JUnit, trajectories and failure diagnostics are
uploaded. Missing/skipped/stale strict reports cannot satisfy gates. CI has
read-only repository permissions, bounded job duration and unconditional
disposable-stack cleanup. See [gate scope and evidence rules](../integrated-ci.md).

September 25, 2026 local validation, base revision
`68a084c` plus this uncommitted change:

- New gate regression tests: **10 passed**, including malformed/empty/skipped/
  failed reports, stale report removal and missing/failed commands.
- Python syntax compilation, workflow YAML parsing and `git diff --check`: passed.
- The isolated runner tests used Windows Python 3.9 with pytest 8.4.2 installed
  under ignored outputs; they do not validate the Python 3.12 application.

The existing virtual environment points to an inaccessible interpreter; Docker
is not available on PATH. Full backend, PostgreSQL, production build, browser,
Compose and remote GitHub Actions execution were not run for this change.
No live-provider, hosted or production restore acceptance is claimed. No commit,
push, deployment, production migration or branch-protection change was made.
