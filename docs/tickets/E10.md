# E10 - Conditional specialist orchestration

Status: backend implementation and deterministic/scientific integration acceptance complete.
Live-provider quality acceptance remains pending under E02; runtime defaults to disabled.

Conditional bounded assignments record objective, criteria, scope, deadline and shared allocation. Independent batches run concurrently under run/project budgets and concurrency; specialists have no tools and cannot delegate. Model-authored handoffs inherit the whole source context, preventing scope relabelling. Interrupted unknown calls retain accounting and are not replayed; completed results survive checkpoint loss. Exact-candidate scientific review is entered through E11/E13 finalization, not ad-hoc delegation.

See [runtime configuration and acceptance scope](../agent-coordinator.md).

September 21, 2026 verification: Linux/PostgreSQL focused suite **157 passed, 1 skipped**
(the SQLite parameter of the PostgreSQL worker-restart test). The preceding scheduler,
run, budget, process and finalization run passed 124 tests with two guard error-code
failures; those were corrected and passed in the 157-test rerun. These suites overlap.
All **49 schema/OpenAPI files** match; Compose environment/startup validation passed.
Final native Windows regression counts are recorded in the coordinator documentation.
No paid provider call, research-database migration, deployment, commit or push occurred.
