# C04 — ChemE baseline and run bundle

Status: implemented and locally verified September 19, 2026. Prerequisite C03 is available. Existing uncommitted C01–C03 changes were preserved.

## Result

The adapter's documented external card/frozen-partition flow was verified with actual public `prepare` and `run_baseline` calls for both workbench-supported models. Tests now check complete bundle inventory, exact original/prepared bytes, card/config/source retention, nested manifests and file hashes, returned run equality, method/seed/parameters/search provenance, prediction digest, and metric reproduction through public upstream scoring. Workbench computes no scientific metrics.

One integration gap was fixed: the pinned benchmark replaces original per-row audit unit rules with constant task-card labels. An additional public-Auditor preflight now checks declared original unit columns against requested canonical units before that replacement. Errors reject admission; modeled-value conversion findings require deliberate curation rather than passing unchanged raw numbers as converted values. Successful preflight evidence is retained in `original-units-audit.json`. No uploaded bytes or upstream package source are changed.

The [integration guide](../benchmark-integration.md) documents this rule, complete outputs, and limits. It explicitly distinguishes validation of supplied declarations from verification of physical/source truth. Unsupported scientific claims are not inferred from a successful run.

## Verification evidence

Environment: constrained Windows ARM Python 3.12.13 in `outputs/c01-venv`; disposable data/cache directories under ignored `outputs/c04-*`.

| Check | Result |
|---|---|
| C04 adapter and worker suite | **15 passed**. Both mean/ridge baselines; complete bundles; public metric verification; absent/unknown/incompatible units and conversion requirements; identity-feature/target-copy/unavailable-feature leakage; group mismatch; provenance gaps; missing source field; explicit warning acceptance. |
| Durable incompatible-unit failure | Included above: audit completes with real findings, split completes, baseline fails with no metrics/bundle, attempted error waiver does not bypass rejection, original download is byte-identical. |
| Workflow, intake, C03 integrity and C01 surface regressions | **74 passed, 6 skipped**. Covers real legacy/Dataset 2.0 workflows, report/replay and upstream integrations. Skips are PostgreSQL-specific cases/parameters without a test database URL. |
| `python scripts/check_scientific_surface.py` | Passed: dependency pins, installed constraints and upstream public snapshot unchanged. |
| `python scripts/export_schemas.py --check` | Passed: all 39 schema/OpenAPI files match. |
| `git diff --check` | Passed. |

Tests report two existing FastAPI/Starlette deprecation warnings. All fixtures are synthetic software validation data. No real research account/database was used.

## Handoff and limits

C07 owns richer unsuccessful-outcome artifacts and actor attribution. C12 owns test exposure/quarantine: complete validation/test metrics and predictions remain in successful outputs. Failed admission retains the existing empty-result/no-bundle representation; it does not fabricate zero metrics. General archive/replay hardening remains C09 work.

Original-unit preflight only checks explicitly declared unit columns. Bare numbers cannot establish the physical truth of a unit label. Upstream validates required source strings/configured provenance findings, not source authenticity or licensing rights. Missing facts still require the existing source admission process.

No public API/schema, dependency pin, migration or frontend change. No commit, push or deployment was performed. The full backend suite, PostgreSQL/Compose/Linux startup, browser checks, hosted CI/deployment, restore and live-provider evaluation were not rerun for this ticket.
